import { AppBar, Box, Typography } from "@mui/material";

const Portfolio = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <AppBar>
        <Typography>Test</Typography>
      </AppBar>
    </Box>
  );
};

export default Portfolio;
